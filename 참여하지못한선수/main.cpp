#include <iostream>
#include <string>
#include <vector>

using namespace std;

string solution(vector<string> participant, vector<string> completion) {
	for(int i = 0 ; i< completion.size(); i++){

		
	}
    return answer;
}

int main(){
	vector<string> participant = {"rlgus", "wlgud", "ehddms", "dlrtn", "rlgusl"};
	vector<string> completion = {"rlgus", "wlgud", "ehddms", "rlgusl"};
	string result = solution(participant, completion);
	cout << result << endl;
	cout << "test" << endl;
	return 0;
}
